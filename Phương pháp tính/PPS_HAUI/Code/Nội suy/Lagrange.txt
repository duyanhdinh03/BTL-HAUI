double Lagrange(int n, double x[], double y[], double X){
    double answer = 0;
    for(int i = 0;i<n;i++){
        double tmp = y[i];
        for(int j = 0;j<n;j++){
            if(i != j) tmp = tmp * ((X - x[j]) / (x[i] - x[j]));
        }
        answer += tmp;
    }
    return answer;
}